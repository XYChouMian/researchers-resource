# 课题组学习资源库 —— 技术方案

## 🎯 需求分析

```
核心功能：
├── 📓 Jupyter Notebook 在线展示/执行
├── 📁 资源文件 上传/下载/管理
├── 👥 用户系统 (课题组成员管理)
└── 📝 Markdown 文档渲染
```

---

## 💡 我的核心建议：不要从零造轮子

> 你的需求高度吻合**已有的成熟开源方案**，直接组合使用远比自己开发更专业、稳定

---

## 🏆 推荐方案：JupyterHub + 文件服务

### 🥇 最推荐方案（直接满足90%需求）

```
┌─────────────────────────────────────────────┐
│                   Nginx                      │
│         反向代理 + SSL(HTTPS)                │
└──────┬──────────────────────┬───────────────┘
       │                      │
       ↓                      ↓
┌─────────────┐      ┌────────────────┐
│  JupyterHub │      │   FileBrowser  │
│  Notebook   │      │   资源下载库   │
│  在线运行   │      │   文件管理     │
└─────────────┘      └────────────────┘
```

---

## 📦 各组件详解

### 组件一：JupyterHub

> **直接解决 Jupyter Notebook 需求**

```bash
# 功能
✅ 多用户登录，每人独立环境
✅ 在线运行/编辑 .ipynb 文件
✅ Markdown + Python 交叉执行
✅ 支持分享notebook链接
✅ 可控制谁能执行、谁只能看
```

### 组件二：FileBrowser

> **直接解决资源库下载需求**

```bash
# 功能  
✅ 网页端文件管理器
✅ 上传/下载/预览文件
✅ 用户权限控制
✅ 界面美观，类似网盘
✅ 支持在线预览PDF/图片
```

### 组件三：Nginx

```bash
# 功能
✅ 统一入口，一个域名访问所有服务
✅ HTTPS证书管理
✅ 反向代理到各子服务
```

---

## 🗺️ 访问结构设计

```
your-domain.com/           → 导航主页（静态页面）
your-domain.com/jupyter/   → JupyterHub（Notebook教程）
your-domain.com/files/     → FileBrowser（资源下载）
your-domain.com/docs/      → (可选) 静态文档站
```

---

## 🚀 部署流程

### Step 1：安装 JupyterHub

```bash
# 安装conda（推荐用于管理Python环境）
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# 安装JupyterHub
conda install -c conda-forge jupyterhub jupyterlab notebook

# 生成配置文件
jupyterhub --generate-config

# 启动测试
jupyterhub
```

### Step 2：安装 FileBrowser

```bash
# 一键安装
curl -fsSL https://raw.githubusercontent.com/filebrowser/get/master/get.sh | bash

# 初始化数据库
filebrowser config init

# 设置根目录（资源库存放位置）
filebrowser config set --root /data/resources

# 创建管理员账号
filebrowser users add admin yourpassword --perm.admin

# 启动测试
filebrowser --port 8080
```

### Step 3：配置 Nginx

```nginx
# /etc/nginx/sites-available/lab-resources
server {
    listen 80;
    server_name your-domain.com;

    # JupyterHub
    location /jupyter/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
      
        # Jupyter必须的WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # FileBrowser 资源库
    location /files/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 静态导航主页
    location / {
        root /srv/www/lab-home;
        index index.html;
    }
}
```

### Step 4：配置 HTTPS

```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

### Step 5：配置系统服务（开机自启）

```bash
# /etc/systemd/system/jupyterhub.service
[Unit]
Description=JupyterHub
After=network.target

[Service]
User=root
ExecStart=/opt/conda/bin/jupyterhub --config=/etc/jupyterhub/jupyterhub_config.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# /etc/systemd/system/filebrowser.service
[Unit]
Description=FileBrowser
After=network.target

[Service]
User=root
ExecStart=/usr/local/bin/filebrowser --port 8080 --root /data/resources
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 启用所有服务
sudo systemctl enable jupyterhub filebrowser nginx
sudo systemctl start jupyterhub filebrowser nginx
```

---

## 🎨 可选增强：导航主页

> 做一个简单的静态HTML主页作为入口

```html
<!-- /srv/www/lab-home/index.html -->
<!DOCTYPE html>
<html>
<head>
    <title>课题组资源中心</title>
</head>
<body>
    <h1>🔬 课题组学习资源中心</h1>
    <div>
        <a href="/jupyter/">
            📓 Notebook 教程中心
        </a>
        <a href="/files/">
            📁 资源下载库
        </a>
    </div>
</body>
</html>
```

---

## 📊 可选扩展方案


| 需求             | 工具                 | 说明                       |
| ---------------- | -------------------- | -------------------------- |
| 静态文档网站     | **MkDocs / Docsify** | Markdown自动生成美观文档站 |
| Notebook只读展示 | **nbviewer**         | 无需登录即可查看notebook   |
| 更强的文件管理   | **Nextcloud**        | 类似私有网盘，功能更全     |
| 团队Wiki         | **Wiki.js**          | 在线协作编辑文档           |

---

## ✅ 方案总结

```
你的需求          →    解决方案
─────────────────────────────────
Notebook教程      →    JupyterHub    ✅
资源下载          →    FileBrowser   ✅
统一域名访问      →    Nginx         ✅
HTTPS安全         →    Certbot       ✅
用户权限管理      →    两者均支持    ✅
开机自动运行      →    Systemd       ✅
```
