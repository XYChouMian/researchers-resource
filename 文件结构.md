# 代码文件存储位置

## 📂 两个服务的存储位置

### 📓 JupyterHub —— 教程Notebook

```
/srv/jupyterhub/notebooks/
├── 每个用户登录后有自己的工作目录
│
└── 实际上每个用户的文件在：
    /home/用户名/
    ├── zhangsan的文件 → /home/zhangsan/
    ├── lisi的文件    → /home/lisi/
    └── wangwu的文件  → /home/wangwu/
```

> ⚠️ **JupyterHub默认每个用户看到的是自己的Home目录**  
> 用户之间文件是**隔离的**

---

### 📁 FileBrowser —— 共享资源库

```
/data/resources/          ← 所有人共享的资源都在这里
├── python教程/
├── 数据集/
├── 工具脚本/
└── 其他资源/
```

---

## 🤔 你可能遇到的问题

> **"我想让大家都能看到同一份Notebook教程，怎么办？"**

默认情况下JupyterHub每人只能看自己的文件，有两个解决方案：

### 方案A：设置共享只读目录 （推荐）

```python
# 在 jupyterhub_config.py 末尾添加
# 让所有用户启动时都能看到共享教程目录

c.Spawner.notebook_dir = '/srv/jupyterhub/notebooks'
```

```bash
# 创建共享目录结构
sudo mkdir -p /srv/jupyterhub/notebooks/shared    # 共享教程（所有人可见）
sudo mkdir -p /srv/jupyterhub/notebooks/workspace  # 个人工作区

# 设置权限：shared目录所有人可读
sudo chmod 755 /srv/jupyterhub/notebooks/shared
sudo chmod 777 /srv/jupyterhub/notebooks/workspace
```

```
/srv/jupyterhub/notebooks/
├── shared/          ← 管理员放教程，所有人可以看
│   ├── 01_Python基础.ipynb
│   ├── 02_数据处理.ipynb
│   └── 03_机器学习.ipynb
└── workspace/       ← 成员自己练习用
    ├── zhangsan_练习.ipynb
    └── lisi_练习.ipynb
```

### 方案B：通过FileBrowser管理教程文件

```bash
# 把Notebook教程也放进资源库
/data/resources/
├── notebooks/        ← Notebook教程文件(.ipynb)
│   ├── 01_Python基础.ipynb
│   └── 02_数据处理.ipynb
└── codes/            ← 其他代码资源
    └── 工具脚本.py
```

> 成员从FileBrowser下载 `.ipynb` 文件  
> 再上传到自己的JupyterHub空间运行

---

## 📊 完整目录结构总览

```
服务器文件系统
│
├── /data/resources/              ← FileBrowser管理（共享资源库）
│   ├── notebooks/                   可上传下载
│   ├── codes/
│   └── datasets/
│
├── /srv/jupyterhub/notebooks/    ← JupyterHub工作目录
│   ├── shared/                      共享教程（管理员维护）
│   └── workspace/                   成员练习区
│
├── /home/用户名/                 ← 各成员私人目录
│   └── 个人文件
│
└── /var/www/lab-home/            ← 导航主页HTML
    └── index.html
```

---

## 💡 我的建议

```
你们课题组的使用场景：

管理员（你）
├── 把教程.ipynb 放到 /srv/jupyterhub/notebooks/shared/
└── 把共享资源   放到 /data/resources/

课题组成员
├── 访问 /jupyter/ → 直接看到并运行教程
└── 访问 /files/   → 下载需要的代码和资源
```

